# Library-Management-System
This Python system uses MySQL to manage library tasks. It allows admins to manage book stock and users to register, search, and borrow books. The script tracks quantities and return dates automatically. It features secure login and auto-creates the database for easy setup.
